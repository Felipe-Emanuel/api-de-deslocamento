import { ReactNode } from "react";

export type Thumbs = {
  cliente: {
    first: ReactNode;
    second: ReactNode;
    firstTitle: string
    secondTitle: string;
  };
  deslocamento: {
    first: ReactNode;
    second: ReactNode;
    firstTitle: string
    secondTitle: string;
  };
  condutor: {
     first: ReactNode;
    second: ReactNode;
    firstTitle: string
    secondTitle: string;
  };
  veículo: {
    first: ReactNode;
    second: ReactNode;
    firstTitle: string
    secondTitle: string;
  };
};
