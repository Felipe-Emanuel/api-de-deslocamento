import { ReactNode } from "react";

export type Thumbs = {
  cliente: {
    secondThumb: ReactNode;
    secondTitle: string;
  };
  deslocamento: {
    secondThumb: ReactNode;
    secondTitle: string;
  };
  condutor: {
    secondThumb: ReactNode;
    secondTitle: string;
  };
  veículo: {
    secondThumb: ReactNode;
    secondTitle: string;
  };
};
